import './agencyheader.scss';

const AgencyHeader = props => {
  return (
    <div className="admin-add-agency-header-container">
      <div className="admin-add-agency-header">
        <p>Add Agency</p>
      </div>
      {/* <span>Add agency and agent</span> */}
    </div>
  );
};

export default AgencyHeader;
